# Backend - Variables de Entorno
# Para Render, copiar esta estructura en Settings > Environment

# Puerto (Render asigna automáticamente)
PORT=5000

# Base de datos Supabase
# Obtén estos valores de: https://app.supabase.com > Settings > Database > Connection String
# La URL tiene este formato:
# postgresql://postgres.XXXX:PASSWORD@db.REGION.supabase.co:5432/postgres
#
# Extrae:
# - DB_HOST = db.REGION.supabase.co (la parte después de @)
# - DB_USER = postgres (siempre es "postgres")
# - DB_PASSWORD = Tu contraseña (la parte después de "postgres.")
# - DB_NAME = postgres (siempre es "postgres")
# - DATABASE_URL = La URL COMPLETA (la más importante)
#
DB_HOST=db.so6dn.supabase.co
DB_USER=postgres
DB_PASSWORD=titojair123***
DB_NAME=postgres
DB_PORT=5432
DB_SSL=true
DATABASE_URL=postgresql://postgres:titojair123***@db.bzdcoezmvttqanuvymtj.supabase.co:5432/postgres

# Correo electrónico (Gmail con contraseña de app)
# Genera en: https://myaccount.google.com/apppasswords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=ezambrano8320@utm.edu.ec
EMAIL_PASSWORD=kblf jgtu lhuz rxnw

# CORS - URL del Frontend (será tu URL de Vercel)
# Para desarrollo local, puedes usar localhost:
# PASO 1 (Primer deploy a Render):
# Usa esto para permitir cualquier origen inicialmente:
CORS_ORIGIN=*
#
# O más seguro, usa localhost para desarrollo:
# CORS_ORIGIN=http://localhost:3000,http://localhost:5173,http://localhost:5174
#
# PASO 3 (After frontend deployed to Vercel):
# Actualiza con tu URL real de Vercel y redeploy:
# CORS_ORIGIN=https://tu-app.vercel.app

# JWT - Genera una cadena aleatoria larga
# Comando: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=8f3d9a2b7e1c4f6d8a9e0b1c2d3f4a5b6c7d8e9f0a1b2c3d4e5f6a7b8c9d0e1f
